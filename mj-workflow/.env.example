# MJ Workflow 环境变量配置
# 推荐：把这些内容放到仓库根目录的 `.env.local`（统一配置，不进 git）

# Yunwu MJ Key（用于 /mj/submit/*）
MJ_API_URL=https://yunwu.ai
YUNWU_MJ_KEY=

# Yunwu ALL Key（用于 /v1/chat/completions 等聚合模型）
YUNWU_ALL_KEY=

# 图床（imageproxy）配置：用于把本地上传的图片转成可访问 URL（给 Gemini/Vision/MJ describe 用）
IMAGEPROXY_API_URL=https://imageproxy.zhongzhuan.chat
IMAGEPROXY_TOKEN=
YUNWU_API_KEY=

# 识图（多模态 /v1/chat/completions）
# 默认使用 YUNWU_ALL_KEY；如需单独配置可启用以下变量（可选）
LLM_API_URL=
LLM_API_TOKEN=
VISION_MODEL=gpt-5.2-chat-latest

# 服务器配置
PORT=3000

# media-backend（FFmpeg 重型任务队列后端）
# 默认不填会使用 http://localhost:9010
PY_MEDIA_BACKEND_URL=http://localhost:9010

# 音频后处理（/api/audio/process）：“抗压缩冗余测试”参数
# - AUDIO_REDUNDANCY_PRESET: off | conservative | distribution
# - distribution 会默认启用轻微的 stereo_delay_ms + noise_dbfs
AUDIO_REDUNDANCY_PRESET=off
# 如需覆盖 preset/接口传参，可用以下变量（可选）
# AUDIO_TEMPO=1.0003
# AUDIO_STEREO_DELAY_MS=0.01
# AUDIO_NOISE_DBFS=-84
