# Sora 视频生成 API 使用说明

Python 客户端,用于 OpenAI Sora 视频生成 API。

## 📦 安装

```bash
pip install requests
```

## 🚀 快速开始

```python
from sora import SoraClient, create_simple_video_request

# 初始化客户端
client = SoraClient(api_token="你的token")

# 创建请求
request = create_simple_video_request(
    model="sora-2",
    prompt="cat dance",
    orientation="portrait",
    duration=15
)

# 生成视频
response = client.generate_video(request)
video_id = response["data"]["id"]

# 查询状态
status = client.query_video(video_id)
```

## 🎬 支持的模型

| 模型 | 说明 | 功能 |
|------|------|------|
| `sora-2` | 标准 Sora 模型 | 文本/图片生视频、角色参考 |
| `sora-2-pro` | 专业版模型 | 所有功能 + 私密模式 |

## 📝 参数说明

### 必需参数

- **model** (str): 使用的模型
- **prompt** (str): 文本提示词

### 可选参数

- **images** (list): 参考图片URL列表 (默认: `[]`)
- **orientation** (str): 视频方向 `portrait`, `landscape`, `square` (默认: `portrait`)
- **size** (str): 视频尺寸 `small`, `medium`, `large` (默认: `large`)
- **duration** (int): 视频时长(秒) (默认: `15`)
- **watermark** (bool): 是否添加水印 (默认: `False`)
- **private** (bool): 私密模式(仅sora-2-pro) (默认: `False`)
- **character_url** (str): 角色参考视频URL
- **character_timestamps** (str): 角色时间戳 (例如: `"1,3"`)

## 💡 使用示例

### 文本生视频

```python
from sora import create_simple_video_request

request = create_simple_video_request(
    model="sora-2",
    prompt="sunset over ocean",
    orientation="landscape",
    duration=15
)
```

### 图片生视频

```python
from sora import create_video_with_images

request = create_video_with_images(
    model="sora-2",
    prompt="make animate",
    image_urls=["https://example.com/image.jpg"],
    orientation="portrait"
)
```

### 角色参考

```python
from sora import create_video_with_character

request = create_video_with_character(
    model="sora-2",
    prompt="make animate",
    character_url="https://example.com/character.mp4",
    character_timestamps="1,3"
)
```

### 私密视频 (Pro)

```python
from sora import create_private_video

request = create_private_video(
    prompt="confidential content",
    orientation="portrait"
)
```

## 🔍 查询状态

```python
from sora import SoraClient, wait_for_video_completion

client = SoraClient(api_token="你的token")

# 查询状态
response = client.query_video("sora-2:task_01kbfq03gpe0wr9ge11z09xqrj")

# 等待完成
result = wait_for_video_completion(client, video_id, timeout=600)
print(f"视频URL: {result['data']['video_url']}")
```

## 🧪 测试

```bash
# 运行示例(干跑模式)
python3 -m sora.sora_examples

# 运行查询示例
python3 -m sora.query_examples
```

## 📊 API 端点

- **生成**: `POST https://yunwu.ai/v1/video/create`
- **查询**: `GET https://yunwu.ai/v1/video/query?id={video_id}`

## 📚 对比

| 特性 | Sora | Runway | Kling | Jimeng |
|------|------|--------|-------|--------|
| 输入 | 文本/图片 → 视频 | 图片 → 视频 | 图片 → 视频 | 文本 → 视频 |
| 模型数量 | 2 (sora-2, pro) | 2 (Gen4) | 2 (v1, v2-6) | 1 (v3.0) |
| 角色参考 | ✓ | ✗ | ✗ | ✗ |
| 私密模式 | ✓ (pro) | ✗ | ✗ | ✗ |
| 水印 | ✓ | ✓ | ✗ | ✗ |
| 查询 | ✓ | ✓ | ✓ | ✓ |

## 🎉 总结

✅ 文本生视频和图片生视频<br>
✅ 角色参考支持<br>
✅ 私密模式(pro)<br>
✅ 多种方向和尺寸<br>
✅ 查询和轮询支持<br>
✅ 完整文档<br>

现在可以使用 Sora 生成视频了！
