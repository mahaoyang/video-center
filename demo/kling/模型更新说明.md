# Kling API 模型更新说明

## 📝 更新内容

**更新日期**: 2025-12-30

### 模型版本更新

已将所有示例和文档中的模型名称从 `kling-v1` 更新为最新的 `kling-v2-6`。

### 支持的模型

- **kling-v1**: 原始模型（仍然支持）
- **kling-v2-6**: 最新模型（推荐使用）⭐

## 🔄 更新的文件

1. **kling_client.py** - 核心客户端
   - 更新文档字符串
   - 添加模型版本说明
   - 更新示例代码

2. **kling_examples.py** - 使用示例
   - 所有示例使用 `kling-v2-6`

3. **test_kling_payload.py** - 测试文件
   - 测试用例使用 `kling-v2-6`

4. **README.md** - 英文文档
   - 所有代码示例更新

5. **使用说明.md** - 中文文档
   - 所有代码示例更新

## ✅ 验证

运行测试确认更新成功：

```bash
python3 -m kling.test_kling_payload
```

输出：
```
✓ Model: kling-v2-6
✓ Valid JSON
✓ Image URL present: True
...
```

## 💡 使用新模型

### 方式 1: 使用辅助函数（推荐）

```python
from kling import create_simple_video_request

request = create_simple_video_request(
    model_name="kling-v2-6",  # 使用最新模型
    image_url="https://example.com/image.jpg",
    prompt="美丽的日落",
    duration="5"
)
```

### 方式 2: 直接创建请求

```python
from kling import KlingVideoRequest

request = KlingVideoRequest(
    model_name="kling-v2-6",  # 使用最新模型
    image="https://example.com/image.jpg",
    prompt="美丽的日落",
    duration="5"
)
```

## 🔄 如果需要使用旧模型

如果仍需使用 `kling-v1`，只需指定模型名称：

```python
request = create_simple_video_request(
    model_name="kling-v1",  # 使用旧模型
    image_url="https://example.com/image.jpg",
    prompt="美丽的日落",
    duration="5"
)
```

## 📊 模型对比

| 特性 | kling-v1 | kling-v2-6 |
|------|----------|------------|
| 状态 | 稳定 | 最新 ⭐ |
| 推荐 | - | ✓ |
| 质量 | 标准 | 更高 |
| 功能 | 基础 | 增强 |

## 🎯 建议

1. **新项目**: 直接使用 `kling-v2-6`
2. **现有项目**: 逐步迁移到 `kling-v2-6`
3. **测试**: 先用小规模测试新模型效果
4. **对比**: 可以同时测试两个模型，选择效果更好的

## 📝 迁移步骤

如果你有使用 `kling-v1` 的现有代码：

1. **全局替换**
   ```bash
   # 在你的项目中
   find . -name "*.py" -exec sed -i 's/kling-v1/kling-v2-6/g' {} \;
   ```

2. **测试验证**
   ```bash
   # 运行你的测试
   python3 -m pytest
   ```

3. **逐步迁移**
   - 先在开发环境测试
   - 对比新旧模型效果
   - 确认无问题后部署到生产

## ⚠️ 注意事项

1. **API 兼容性**: 两个模型使用相同的 API 接口
2. **参数相同**: 所有参数在两个模型中都适用
3. **费用可能不同**: 新模型可能有不同的计费标准
4. **效果差异**: 新模型可能产生不同的视频效果

## 🔍 检查当前使用的模型

```python
from kling import KlingVideoRequest

request = KlingVideoRequest(
    model_name="kling-v2-6",
    image="https://example.com/image.jpg"
)

print(f"使用的模型: {request.model_name}")
# 输出: 使用的模型: kling-v2-6
```

## 📚 更多信息

- 查看完整文档: `kling/README.md`
- 查看使用示例: `kling/kling_examples.py`
- 运行测试: `python3 -m kling.test_kling_payload`

## ✨ 总结

✅ 所有文件已更新为 `kling-v2-6`
✅ 保持向后兼容 `kling-v1`
✅ 测试通过
✅ 文档已更新

现在可以使用最新的 `kling-v2-6` 模型了！
